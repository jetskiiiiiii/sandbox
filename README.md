# PRACTICES
- git practices
1. remote branches will be removed once merged
2. if you are making changes and find that you have been working on wrong branch, google it (different based on commited/uncommitted)
3. before pushing local branch, update local main (and thus the local branch)
4. instead of git pull, use git fetch + git log <local-branch>..<remote-branch> + git merge
5. name branches with "high-level/low-level" syntax

- general practices
1. when authenticating pages, create separate auth component and wrap component in Suspense in page.tsx
    - https://nextjs.org/docs/messages/blocking-route
2. for now, authenticate using getUser

# TODO (Rio):
  - Darrel
    1. after signup take them straight to homepage
    2. in account settings, add button to verify email
  - Rio
    - create-post
        - post images
            - add caption to images
        - (DONE) currently public
        - function to save draft
            - state of draft should be saved
                - (DONE) bucket should differentiate between drafts, allowing user to upload same image to multiple drafts
        - build canvas to host tiles
        - build display

        - post text/video
        - add ui
        - (DONE) redirect to profile homepage after upload
        - constraing status column in supabase to "draft" or "published"

# FLOWS 
    - Creations Dashboard
        - has published and drafts in "all" organized by date
            - tabs for published and draft
        - each creation has a unique id
            - if id exists, load draft
            - else, load empty 
        - each creation is a canvas where users can put texts, images, videos anywhere
        - 
    - Post layout
        - idea 1: freeform, put text/images/videos anywhere, floating elements with editors for each specific element type
            - pros: cool web experience, difficult mobile implementation   
        - idea 2: blocked elements in semi-freeform grid placements
            - pinterest-like arrangment with each post as a collection of videos/images/texts
    - creations database flow:
        - tables: metadata, tile state, post state, item ids (image/video/text)
            - metadata (currently 'posts_metadata'): id, user_id, created_at, updated_at, title
                - should be: id, user_id, created_at, updated_at, title, authors, public_url, state
            - post state: id, tile_ids[], tile_positions[]
            - tile state (currently 'draft_images'): id, user_id, post_id (posts_metadata - id), created_at, image_ids[]
                - should be: id, user_id, post_id, created_at, updated_at, type, title, item_ids 
            - item ids (currently 'images'): id, user_id, created_at, public_url, status
                - should be: id, user_id, created_at, public_url, description
        - when creating post, generate post metadata and empty post state. then when user saves, upsert post state, tile state, item ids
            -   

# IDEAS
- video only section to scroll through all videos/images-as-videos(autogenerated) on platform
    - include "Part of [POST NAME] by [USERNAME]"
- social media collaboration

# REFERENCES:
- https://dev.to/bolajibolajoko51/how-i-built-a-custom-image-file-uploader-5aje
- uncached data accessed outside of suspense: https://nextjs.org/docs/messages/blocking-route

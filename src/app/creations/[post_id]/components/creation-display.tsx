"use client"

import { CreationDisplayPropType } from "@/utils/types/interface"

/*
 * Retrieve and display creation tiles.
 */
export default function CreationDisplay({
  user_id,
  creation_id,
  updated_at,
  status,
  creation_title,
  date_published,
  tile_ids,
  tile_positions
} : CreationDisplayPropType) {

  return (
    <div>
      Display
    </div>
  )
}
